services:
  udp-to-websocket:
    image: harbor.gladiators.dev/library/udp-to-websocket:latest
    network_mode: "host"
    #restart: on-failure
    container_name: udp-to-websocket
    environment:
      - TEST_MODE=1
    # ports:
    #   - "8081:8081/udp"
